# === 1. Windows 遗留产物 (保持忽略) ===
.vs/
x64/
Debug/
Release/
*.user
*.suo
*.db
*.opensdf
*.sdf
*.ipch
*.obj
*.pdb
*.ilk
*.tlog
*.log
*.idb

# === 2. 树莓派编译中间产物 (保留) ===
*.o
*.out

# === 3. 特殊修改：允许上传 build 和 SDK ===
# 注意：下面这两行必须确保前面没有其他规则冲突
!build/
!SDK/

# 如果你之前写了 build/ 这一行，请务必将其删掉或注释掉
# build/  <-- 这一行必须消失

# === 4. 其他临时文件 ===
.DS_Store
*.swp

# === 5. Python 工程专用忽略规则 ===
# Python 字节码缓存 (最重要)
__pycache__/
*.py[cod]
*$py.class

# Python 虚拟环境 (如果你在本地创建了 venv)
venv/
.venv/
env/
ENV/

# PyCharm / VSCode 配置 (可选，防止IDE配置上传)
.idea/
.vscode/

# Python 打包产生的临时文件夹 (如果有 setup.py)
*.egg-info/
dist/
# 注意：Python 打包有时也会生成 build/，但你前面已经强制保留了 !build/
# 如果 Python 工程是在子目录 PC_Server_Python 下，互不影响。