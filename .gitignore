# ====================================
# TE-GS Project Git Ignore Rules
# ====================================

# === 1. Windows/Visual Studio 遗留产物 (忽略) ===
.vs/
x64/
Debug/
Release/
*.user
*.suo
*.db
*.opensdf
*.sdf
*.ipch
*.obj
*.pdb
*.ilk
*.tlog
*.log
*.idb

# === 2. 编译中间产物 (忽略) ===
# 通常我们不上传 .o 和 .out 文件，保持仓库轻量
*.o
*.out

# === 3. 特殊白名单：强制上传 build 和 SDK ===
# Git 默认不忽略文件夹，除非上面有规则覆盖。
# 这里加感叹号是双重保险，确保这两个关键目录被提交。
!build/
!SDK/

# === 4. 操作系统/编辑器临时文件 (忽略) ===
.DS_Store
*.swp
.idea/
.vscode/

# === 5. Python 工程专用规则 (忽略) ===
# 字节码缓存 (绝对不要上传这些)
__pycache__/
*.py[cod]
*$py.class

# 虚拟环境 (本地环境不上传)
venv/
.venv/
env/
ENV/

# 打包分发临时文件
*.egg-info/
dist/
# 注意：Python打包通常也会生成 build/，但因为上面第3条我们强制保留了 build/
# 所以这里 Python setup.py 生成的 build 也会被上传。
# 如果这造成困扰，请手动管理。